import 'package:flutter/material.dart';

class WidgetColosNote extends StatelessWidget {
  const WidgetColosNote({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column();
  }
}
